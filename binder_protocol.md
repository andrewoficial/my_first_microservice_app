# Binder — климатическая камера (TCP, Modbus-подобный фрейм)

Рабочий протокол. Проверено на штатной программе управления камерой Binder —
управление и чтение работают корректно.

Исходники (кодек/каналы):
- кодек: `device/ethernet/binder/BinderCommandRegistry.java`, `BinderModbus.java`
- клиент (панель управления): `gui/devices/binder/camera/control/`
- сервер (эмулятор): `gui/devices/binder/camera/emulation/`

## Транспорт
- Соединение: **TCP**, порт по умолчанию **10001** (host по умолчанию `10.0.7.9`).
- Таймаут подключения 3000 мс, чтения 2000 мс, `TCP_NODELAY = true`.
- slaveId по умолчанию = **1**.

## Контрольная сумма
CRC-16/Modbus (полином `0xA001`), в конце кадра, **младший байт вперёд**:
```
frame[n]     = crc & 0xFF      // младший байт
frame[n + 1] = crc >>> 8       // старший байт
```
CRC считается по кадру БЕЗ собственных 2 байт CRC.

> ВАЖНО (историческая грабля): в старой C++-версии (CBinderTCamera) CRC писали старшим
> байтом вперёд (HIBYTE/LOBYTE) — камера не отвечала. Правильно — little-endian.

## Карта регистров
| Адрес | Направление | Поле | Кодировка |
|------:|:---:|------|-----------|
| `0x1581` | запись (0x10) | уставка температуры | 32-бит float °C |
| `0x11A9` | чтение (0x03) | текущая температура | 32-бит float °C |
| `0x158B` | запись (0x06) | влажность вкл/выкл | `0x81`=вкл, `0x00`=выкл |

## Команды

### 1. SetT — установка температуры (`0x10`)
Запись 2 регистров с `0x1581`. Значение — **32-бит float IEEE-754** °C.

| Смещение | Размер | Значение |
|---------:|:---:|----------|
| 0 | 1 | slaveId |
| 1 | 1 | `0x10` |
| 2 | 1 | `0x15` (старший байт регистра) |
| 3 | 1 | `0x81` (младший байт) → `0x1581` |
| 4 | 1 | `0x00` |
| 5 | 1 | `0x02` (число регистров) |
| 6 | 1 | `0x04` (число байт данных) |
| 7 | 1 | float byte 1 |
| 8 | 1 | float byte 0 |
| 9 | 1 | float byte 3 |
| 10 | 1 | float byte 2 |
| 11..12 | 2 | CRC-16 |

Байты float (`Float.floatToRawIntBits`, b0 — самый младший): в кадр идут `b1,b0,b3,b2`
(по 2 байта на регистр, little-endian). Длина кадра **13** байт.

**Ответ:** 8 байт-эхо: `[slave][0x10][0x15][0x81][0x00][0x02][crc][crc]`.

### 2. GetT — чтение температуры (`0x03`)
Чтение 2 регистров с `0x11A9`:

| Смещение | Размер | Значение |
|---------:|:---:|----------|
| 0 | 1 | slaveId |
| 1 | 1 | `0x03` |
| 2 | 1 | `0x11` |
| 3 | 1 | `0xA9` → `0x11A9` |
| 4 | 1 | `0x00` |
| 5 | 1 | `0x02` |
| 6..7 | 2 | CRC-16 |

**Ответ:** 9 байт: `[slave][0x03][byteCount=0x04][b3][b2][b1][b0][crc][crc]`.
Int собирается и конвертируется в float:
```java
int bits = (r[4]&0xFF) | ((r[3]&0xFF)<<8) | ((r[6]&0xFF)<<16) | ((r[5]&0xFF)<<24);
float t = Float.intBitsToFloat(bits);
```

### 3. SetHC — влажность вкл/выкл (`0x06`) — авто
Функция `0x06`, регистр `0x158B`:
`[slave][0x06][0x15][0x8B][0x00][0x81|0x00][crc][crc]` — `0x81`=вкл, `0x00`=выкл.
**Ответ:** 8 байт-эхо `[slave][0x06][0x15][0x8B][0x00][val][crc][crc]`.

Автоправило (включается на каждом успешном GetT):
- `temp >  -6 °C` → влажность ВКЛ (`0x81`);
- `temp < -12 °C` → влажность ВЫКЛ (`0x00`);
- `-12 .. -6 °C` → без изменений.
Состояние кэшируется (`humidityControlOn`), команда не повторяется без изменения.

## Надёжность / разбор ответов
- Короткий ответ / ошибка чтения → повтор до 3 раз.
- Неверная CRC → ответ отбрасывается, повтор.
- Весь обмен журналируется в hex (`→ [SetT] ...`, `← [GetT] ...`).

## Эмулятор (динамика)
- Фактическая температура сходится к уставке со скоростью `rampRateDegPerMin`
  (по умолч. 5 °C/мин), с «перелётом» `overshootDeg` (по умолч. 2 °C) и флуктуацией
  на полке `fluctuationAmpDeg` (0.3 °C) периода `fluctuationPeriodSec` (20 с).
- Рабочий диапазон `-40 .. +120 °C` (уставка клампится).
- Ответы и эхо считаются тем же кодеком, что и клиент (один источник правды).

## Известные нюансы
- Значения температуры идут как float, а не целые/масштабированные — при разборе важно
  восстанавливать порядок байт именно как `b3,b2,b1,b0` → int → float (не «наивный» BE).
- Для реальной камеры адрес/порт задаётся при подключении; эмулятор слушает `0.0.0.0:10001`.
