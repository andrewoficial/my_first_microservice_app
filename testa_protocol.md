# Testa — климатическая камера (UDP)

Реализация по образцу C++ `CTestaTCamera` / Java `TestaChamber`.
Исходники: `device/ethernet/testa/TestaCommands.java` (кодек),
`gui/devices/testa/` (клиент управления + эмулятор).

> Часть карты статуса и команды получены **эмпирически** — подбором байт/битов против
> штатной (фирменной) программы Qt (`181102.exe`). Названия некоторых полей и границы
> «неопределённых» байт могут быть неточны и требуют подтверждения.

## Транспорт
- Протокол: **UDP** (дейтаграммы).
- Команда SetT шлётся на порт камеры **1300** (`host:1300`).
- Статус принимается на локальном порту **1200**.
- Схема обмена (важно для эмулятора):

```
контроллер (сокет, привязан к :1200)
   │── UDP →  camera:1300   (SetT/команды)
   │←─ UDP ─  camera → контроллер:1200  (статус, 40 байт, периодически ≈1 Гц)
```
Камера сама периодически шлёт статус на порт 1200 клиента (отдельного запроса GetT нет —
статус приходит «пакетом»). Чтобы камера узнала адрес клиента, клиент сначала шлёт SetT
(из её адреса берётся IP, ответ уходит на `IP:1200`).

### Сокеты / порты
- Контроллер: один `DatagramSocket`, привязан к `:1200`, шлёт и принимает на нём.
- Камера (эмулятор): слушает `:1300` (приём команд/SetT), шлёт статус на `клиентIP:1200`.

## Установка температуры (SetT) — 32 байта
| Смещение | Значение |
|---------:|----------|
| 0..3 | `0x02 0x33 0x88 0x66` |
| 4 | не заполняется |
| 5 | 0x01 = работа с влагой активна (при «запуске»; иначе 0) |
| 6..7 | этим кадром штатка не трогает |
| 8..11 | float `68.0` (служебный, штатка ставит константу) * |
| 12..15 | температура, 32-бит float IEEE-754, **little-endian** |
| 16..19 | скорость выхода °/мин, float LE (**0 = максимум**) |
| 20..23 | dword `33 00 00 00` (байты 21..23 в этом кадре нули) * |
| 24..27 | этим кадром штатка не трогает |
| 28..31 | уставка влажности %RH, float LE (например `00 00 48 42` = 50.0) |

Float упаковывается как `Float.floatToRawIntBits`, little-endian:
```
byte[12] =  bits & 0xFF
byte[13] = (bits >>> 8) & 0xFF
byte[14] = (bits >>> 16) & 0xFF
byte[15] = (bits >>> 24) & 0xFF
```
(*) Поля подтверждены дизассемблированием `181102.exe`: сборщик `0x462042` заполняет 5, 8..11,
16..19, 20..23, 28..31. Обычный простой SetT заполняет только 12..15. CRC нет, ответ на SetT не
читается.

**Заголовок программы (второе семейство уставки), режим «программа»:** при внутреннем режиме
`== 2` штатка шлёт на `:1300` кадр с заголовком **`77 55 33 88`** + дописанным расписанием
(тоже как «уставка», длина ≥32). Эмулятору стоит узнавать оба заголовка: `02 33 88 66` и
`77 55 33 88`.

## Статус камеры (GetT-данные) — 40 байт
Принимается датаграмма ровно **40 байт**, заголовок `0x11 0x22 0x00 0x00`
(датаграммы иного размера/заголовка игнорируются).

Все 16-битные значения — **знаковые short, little-endian, /100** (°C/%RH/давлец/прочее).

| Смещение | Поле | Примечание |
|---------:|------|-----------|
| 0..3 | заголовок `11 22 00 00` | |
| 4..5 | текущая температура, °C | |
| 6..7 | уставка, °C | |
| 8..9 | управление температурой, % | short /100 |
| 10..11 | не определено | наблюдалось `00 00` |
| 12..13 | влажность текущая, %RH | short /100 |
| 14..15 | влажность заданная, %RH | |
| 16..17 | управление влажностью, % | |
| 18..19 | не определено | наблюдалось `00 00` |
| 20 | выходы контура 404 (биты) | см. ниже |
| 21 | выходы контура 23 + подсветка (биты) | см. ниже |
| 22 | аварии/ошибки (биты) | см. ниже |
| 23..25 | не определено | |
| 26..27 | температура парогенератора, °C | |
| 28..29 | датчик 22 — целевое значение | |
| 30..31 | не определено | |
| 32..33 | датчик 404 — текущее | |
| 34..35 | датчик 23 — текущее | |
| 36..37 | датчик 404 — целевое | |
| 38..39 | датчик 23 целевое (атм) | |

Разбор короткого:
```
short a = (short)w;   °C  = a / 100.0
```
**Подсказка формата short /100:** если при тесте записать `0x7C` в *младший* байт пары — поле
покажет ≈1.24; если `0x7C` в *старший* — ≈317.4 (`0x7C00` = 317.44). Это помогает опознать пару
байт и их порядок.

> Уточнения из дизассемблирования `181102.exe` (декодер `fromPLC`, `0x45EE10`):
> - байты **10..11**, **18..19**, **30..31** — тоже `int16 /100`, не «дыры» (нулевые `00 00`
>   = 0.0, потому в UI «тихо»);
> - байты **20..23** — один сырой `uint32` флагов (байт 23 входит в это же слово, отдельно не
>   разбирается);
> - байты **24..25** — единственный «не /100» после заголовка (сырой `uint16`) — кандидат на
>   поле режима/состояния запуска.

### Битовые поля
**Байт 20 — выходы контура 404:**
| Бит | Назначение |
|----:|-----------|
| 0 | Вентиляция |
| 1 | Подогрев двери |
| 2 | Подогрев стекла |
| 3 | Работа со влагой |
| 4 | Соленоид 404 малый |
| 5 | Компрессор 404 |
| 6 | Соленоид 404 основной |
| 7 | Соленоид 404 перепуск |

**Байт 21 — выходы контура 23 / подсветка:**
| Бит | Назначение |
|----:|-----------|
| 0 | Осушитель |
| 1 | Соленоид 404 на ТО |
| 2 | Компрессор 23 |
| 3 | Соленоид 23 основной |
| 4 | Соленоид 23 перепуск |
| 5 | Подогрев 23 |
| 6 | не определено (нет реакции) |
| 7 | Подсветка камеры |

**Байт 22 — аварии/ошибки:**
| Бит | Назначение |
|----:|-----------|
| 0 | «Мало воды в парогенераторе» |
| 1..7 | не определено |

## Команды от штатной программы к камере (RX на :1300)
Штатка работает как клиент и шлёт камере команды; эмулятор должен их принимать. Пока опознано
**два** семейства:

1. **SetT / «запуск» — `02 33 88 66` (32 байта)** — см. выше (несёт температуру, скорость,
   влажность). Эмулятор узнаёт его и ставит уставку.
2. **Служебные/кнопочные — `23 45 34 21` (12 байт)**:
   ```
   [0..3]  23 45 34 21
   [4]     opcode (quint8)
   [5..7]  мусор (стек, код не пишет — игнорировать)
   [8..11] param (uint32 LE)
   ```
   Длина жёстко `0x0C`. Примеры (мусор в 5..7 игнорируем):
   - остановка поддержания: `23 45 34 21 64 xx xx xx 01 00 00 00` → op `0x64`, param `1`
   - подсветка камеры вкл:   `23 45 34 21 66 xx xx xx 01 00 00 00` → op `0x66`, param `1`
   Опкоды (константы штатки): `0x64` стоп/state=0, `0x66` toggle (подсветка/тумблеры),
   `0x67` служебный (после открытия настроек), `0x6A` состояние/режим. Ручные тумблеры
   (вентиляция, дверь, компрессоры…) идут этим же путём — карту «op → выход» снимать
   сниффером по **байту 4 и байтам 8..11**.

## Переустановка уставки (цикл испытания)
Уставку нужно посылать повторно, если:
- уставка ещё не задавалась (`dTSet == -1000`), либо
- заданная уставка (`dTSet`) не равна считанной из камеры (`dTSetRead`).

Т.е. контроллер отправляет SetT до тех пор, пока камера не подтвердит уставку в статусе.

## Динамика эмулятора (модель поведения)
Эмулятор имитирует не просто статичный кадр, а «живую» камеру:

- **Режим** `running`: true при приёме `02 33 88 66` (SetT) или `77 55 33 88` (программа), false при
  кнопке стоп (`23 45 34 21`, op `0x64`).
- **Температура**: пока `running` — текущая идёт к уставке со скоростью выхода (`rampRate`);
  когда остановлена — **медленно дрейфует к 25 °C** с фиксированной малой скоростью
  (~0.5 °C/мин), независимо от заданной скорости.
- **Влажность**: моделируется как температура — **текущая плавно идёт к заданной**
  (~2 %RH/мин). Заданная берётся из SetT (float RH, байты 28..31) или поля «Влажность заданная»
  (байты 14..15). Каждый статус пишет текущую RH в байты **12..13**, заданную в **14..15**.
- **Подсветка**: при кнопке `23 45 34 21` op `0x66` ставится/снимается бит 15 слова флагов
  (= байт 21, бит 7) в статусе.
- **ACK**: на `02 33 88 66`/`77 55 33 88` → `72 34 02`, на `23 45 34 21` → `72 34 01`
  (на `:1200` клиента). Без ACK штатка повторяет команду (~600 мс, до ~6 попыток).
- **Состояние «работа/стоп»** в 40-байтном статусе штатка **не читает** — её кнопки/иконки это
  локальная Qt-машина состояний (`State::stateEnum`, зависит от внутреннего `mode`, не от UDP).
  Поэтому эмулятор не может и не должен «переключать» кнопки Start/Stop у `181102.exe`.

## Отладка со штатной программой (внешний клиент)
Штатная (фирменная) программа управления камерой — Qt (`181102.exe`, дефолтный адрес камеры
зашит как **10.0.6.10**) ходит к камере так:

- **Слушает** статус **широковещательно** — поэтому она видит статус от эмулятора даже без
  знания его адреса.
- **Пишет** (команды, в т.ч. кнопки) **только** в адрес камеры **10.0.6.10** (взятый из её
  настроек). Если этого IP нет на машине — пакеты не доходят до эмулятора.

Чтобы эмулятор на локальной машине принимал команды штатной программы, назначить `10.0.6.10`
локально как loopback-алиас (права администратора):

```powershell
netsh interface ipv4 add address "Loopback Pseudo-Interface 1" 10.0.6.10 255.255.255.255
```

Убрать алиас:

```powershell
netsh interface ipv4 delete address "Loopback Pseudo-Interface 1" 10.0.6.10
```

Имя интерфейса (часто `Loopback Pseudo-Interface 1`) уточнить через `ipconfig`. Осторожно: если
в сети реально есть камера на `10.0.6.10`, будет конфликт — тогда лучше в настройках штатной
программы указать IP самого ПК.

### Заводской пароль настроек
Пароль на экран настроек штатной программы — **8520** (заводской; в exe лежит обычным ASCII
`"8520"`; поле числовое 0..99999).

### Инструменты в эмуляторе
- **Сниффер UDP** (блок «Сниффер UDP») — слушает диапазон портов (до 65535) на NIO-Selector и
  пишет в лог любую дейтаграмму: `SNIFF ← ПОРТ XXXX от IP (N б): hex…`. Помогает найти порт и
  формат кнопочных команд.
- **Отладка байт** — по-байтовый/по-битовый редактор полей статуса с кнопками «все биты 0/1»,
  навигацией «< предыдущий/следующий адрес >» и галкой «показывать все адреса (вкл. известные)».
- **Ручной кадр** (галка в блоке «Отладка байт») — эмулятор слаёт статус байт-в-байт из
  заданного пользователем кадра **0..39**, не перезаписывая заголовок и температуру. Нужен, чтобы
  подбором находить поле «состояния запуска» и прочие ещё не расшифрованные байты. Заголовок
  `11 22 00 00` (байты 0..3) должен сохраняться, иначе кадр не распознаётся.

## Реализация в проекте
- **Кодек** `TestaCommands`: `buildSetTemperatureFrame(float)` (32B),
  `isTemperatureDatagram(byte[])`, `parseActual`, `parseSetpoint`,
  `buildStatusDatagram(actual, setpoint [, humCur, humSet] [, rawBytes8..39])` (40B),
  `getStatusDatagramLength()`.
- **Эмулятор** (`gui/devices/testa/emulation`): `TestaServerService` (UDP :1300, рассылка статуса
  на :1200, ACK `72 34`, разбор SetT/программы/кнопок `23 45 34 21`), `TestaEmulator`
  (динамика температуры и влажности, дрейф к 25 при остановке, режим ручного кадра, свет),
  `TestaUdpSniffer` (сниффер), панель/окно.
- **Управление** (`gui/devices/testa/control`): `TestaCommunicationService` (приём :1200,
  SetT на host:1300), `TestaControlPanel`.
- Меню: «Панели управления → Testa → Панель управления / Панель эмуляции».

Для проверки пары: запусти эмулятор (UDP :1300), затем панель управления
(host `127.0.0.1`, port 1300) — задай уставку, текущая температура должна подойти к ней,
а в логе виден UDP-обмен (SetT → статус).
