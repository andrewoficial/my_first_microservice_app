# Карта регистров термокамер BOTO 800 / BOTO 1000 (эмуляция, Modbus RTU)

Получено опытным путём (маппинг «адрес → значение») по штатной программе
`UApp_boxed.exe`. Масштаб/интерпретация каждого поля указаны по факту отображения в GUI.

Сеть: Modbus RTU, **9600 8N1**, адрес устройства **1**.

## Функции
- `0x03` — чтение holding registers
- `0x06` — запись одного регистра (отвечается echo)
- `0x10` — запись нескольких регистров (отвечается echo) — штатная программа пишет ею
  даже одиночные регистры. Для одной записи кадр: `01 10 reg(2) 0001 02 val(2) crc(2)`.

## Блок «реального времени» — читается штатной программой одним блоком
Запрос: `01 03 00 0A 00 28` (адрес 10, 40 регистров = 10..49). База TEMP_PV = рег **10**.

| Адрес | Поле | Отображение | Примечание |
|------:|------|-------------|-----------|
| 10 | TEMP_PV | °C (raw/10) | текущая температура |
| 11 | TEMP_SP | °C (raw/10) | уставка температуры |
| 12 | (статус/режим?) | 0/1 | зеркало вкл/выкл |
| 13 | MV темп. | 0..100 | степень коррекции температуры |
| 14 | HUMI_PV | % (raw/10) | текущая влажность |
| 15 | HUMI_SP | % (raw/10) | уставка влажности |
| 16 | (статус/режим?) | 0/1 | зеркало вкл/выкл |
| 17 | MV влаги | 0..100 | степень коррекции влажности |
| 31 | Камера вкл/выкл | 0/1 | 1 = работа (поддержание), 0 = стоп |
| 32 | RUN time H | часы | накопленное время работы |
| 33 | RUN time M | минуты | |
| 34 | RUN time S | секунды | |
| 38 | Нижний предел темп. | °C (знаковый /10) | -5 → -0.5, +1 → +0.1 |
| 39 | Предел уставки темп. | °C (raw/10) | верхний предел ввода температуры |
| 40 | Нижний предел влаги | % (raw/10) | 4 → 0.4 |
| 41 | Предел уставки влаги | % (raw/10) | верхний предел ввода влажности |
| 49+ | ... | — | не определено |

> ВАЖНО: если эмулятор на неизвестные адреса возвращает 0, штатная программа берёт
> предел = 0 и не даёт ввести уставку. Рег 39/41 должны возвращать реальные пределы.

## Регистры записи уставок (команды)
Штатная программа **не читает** их обратно, а читает зеркала из блока реального времени.

| Адрес | Поле | Масштаб |
|------:|------|---------|
| 60 | уставка температуры (0x3C) | °C (raw/10) |
| 61 | уставка влажности | % (raw/10) |
| 63 | режим: 1 = работа, 0 = стоп | 0/1 |

Запись часто идёт парой рег 60+61: `01 10 00 3C 00 02 04 <t> <h> crc`.

## Тест / программа (состояние испытаний)

| Адрес | Поле | Масштаб/вид |
|------:|------|-------------|
| 61 | установленная точка влажности | % (raw/10) |
| 62 | номер текущей программы испытаний | 1:1 |
| 70 | SEG n/m — M (всего сегментов) | 1:1 |
| 71 | SEG n/m — N (текущий сегмент) | 1:1 |
| 72 | CYCLE n/m — N (текущий цикл) | 1:1 |
| 73 | CYCLE n/m — M (всего циклов) | 1:1 |
| 74 | RMN TIME H | часы остатка |
| 75 | RMN TIME M | минуты остатка |
| 76 | RMN TIME S | секунды остатка |
| 111 | Prog wait set TEMP | °C (raw/10) |
| 112 | Prog wait set HUMI | % (raw/10) |

## Часы

| Адрес | Поле | Вид |
|------:|------|-----|
| 92 | SYSTEM TIME — год | |
| 93 | SYSTEM TIME — месяц | |
| 94 | SYSTEM TIME — день | |
| 95 | SYSTEM TIME — час | |
| 96 | SYSTEM TIME — минута | |
| 97 | SYSTEM TIME — секунда | |
| 151 | Reserve time — год | |
| 152 | Reserve time — месяц | |
| 153 | Reserve time — день | |
| 154 | Reserve time — час | |
| 155 | Reserve time — минута | |
| 156 | Reserve time — секунда | |

## Прочие/скорости
| Адрес | Поле | Масштаб |
|------:|------|---------|
| 101 | TIME SET H.M | raw/100 (22 → 0.22) |
| 102 | C/M темп (скорость) | (raw/10) |
| 103 | %/M влага (скорость) | (raw/10) |
| 200 | номер подпрограммы испытания | 1:1 |
| 201 | repeat (число повторов?) | 1:1 |
| 202 | LINK | 1:1 |

## Полки (TS — temperature shelves) — НЕ выносить в GUI, только задокументировать
Формат группы полки (например полка 1):
- `250` — температура полки (raw/10; 11 → 1.1)
- `251` — влажность полки (raw/10; 11 → 1.1)
- `252` — H.M полки (raw/100; 11 → 0.11)
- `253` — TS1: 0=OFF, 1=ON, >1 = период/цикл/интервал
- `254` — TS2: 0=OFF, 1=ON, >1 = период/цикл/интервал

Полки:
- Полка 1: 250..254
- Полка 2: 255..259  (255 темп, 256 влажность, 257 H.M, 258 TS1, 259 TS2)
- Полка 3: 260..264
- Полка 4: 265..269
- ... шаг +5 на полку, до 100 полок. Последняя полка ≈ 745/746/747.

> В штатной программе TS3/TS4 всегда показывают OFF независимо от значения регистра —
> вероятно GUI жёстко рендерит только 2 TS на группу, либо читается не тот адрес.
> Требует уточнения (пока не разобрано).

## Настройки устройства (протокол из `Программа.txt`)
```
#define MB_ADR 1
#ifdef CAM1                 // "big" = BOTO 800
    REG_TEMP=10   REG_SET_TEMP=60   REG_SET_MOD=63   SCALE=10
#else                       // = BOTO 1000 / China Modbus
    REG_TEMP=12   REG_SET_TEMP=100  REG_SET_MOD=105  SCALE=100
#endif
```
REG_SET_MOD: 1 = работа (ON), 0 = стоп (OFF).

## Известные нюансы
- Маппинг «адрес как значение» на служебных блоках (0x22C4, 0x2304, 0x157E, 0x1FAC и др.)
  даёт программе абсурдные пределы (0..3.90 темп / 0..4.10 влага). В нормальном режиме
  такие блоки надо отдавать нулями или правдоподобными значениями.
- Источник значений рег 19/20 («Контроль наблюдения темп/влаги») пока не найден — не критично.

## Наблюдались, но не опознаны (для продолжения)
Регистры, которые штатная программа реально читает (и/или которые проявлялись при маппинге),
но смысл которых пока не выяснен. Удобно начинать с этих адресов при следующем заходе.

### 1. Пробелы внутри блока реального времени (рег 10..70)
Программа читает блок `01 03 00 0A 00 3D` = рег **10..70** одним запросом. Опознаны:
10-17, 31, 32-34, 38-41, 60-63, 70. **Непокрыты (эмулятор отдаёт 0):**
- `18..30` — плотный блок; точно есть 19/20 («контроль наблюдения темп/влаги») и, вероятно,
  рядом 18/21+ (Time set / аварии / сохранённые пределы / статусы).
- `35..37` — между RUN time (32-34) и нижним пределом темп (38).
- `42..59` — крупный нераспознанный участок (включая 50-59) до уставок 60-63.
- `64..69` — между рег 63 (режим) и рег 70 (SEG total).
- `71..` за пределом блока (SEG total=70) — отдельные поля ниже.

### 2. Отдельные служебные чтения (появляются в логе обмена)
Эти блоки программа периодически читает отдельными запросами. Сейчас эмулятор отдаёт их нулями.
Назначение адресов/блоков не установлено:
- `01 03 00 4D 00 01` — одиночный рег **0x4D (77)**.
- `01 03 1F AC 00 01` — рег **0x1FAC (8108)**. Пишется через `0x10` в `1F AC` значением 0/1
  (похоже на команду/флаг запуска или сброса).
- `01 03 15 7E 00 10` — блок **0x157E..0x158D (5498..5517)**, 16 регистров.
- `01 03 22 C4 00 40` — блок **0x22C4..0x2303 (8900..8963)**, 64 регистра.
- `01 03 23 04 00 1A` — блок **0x2304..0x231D (8964..8989)**, 26 регистров.
> ВАЖНО: при маппинге (адрес=значение) эти высокие адреса возвращают огромные числа и ломают
> GUI (абсурдные пределы). В штатном режиме — только 0 или правдоподобные значения.

### 3. Смысл не до конца ясен, но значения «проходят 1:1»
- `201` repeat — предположительно число повторов; назначение не подтверждено.
- `202` LINK — назначение неизвестно.
- `200` номер подпрограммы испытания — распознано, но подпрограмма в GUI не изучалась.
- `101` TIME SET H.M — формат h.m; логика/единица неясна (22 → 0.22).
- `12` и `16` — в эмуляторе зеркалят «вкл/выкл», но GUI статус камеры по ним НЕ менялся;
  фактический бит «камера включена» оказался рег **31**. Настоящий смысл 12/16 неизвестен.
- `19`, `20` «Контроль наблюдения темп/влаги» — виден в GUI, источник регистра не подтверждён.
- TS3/TS4 всегда OFF в GUI независимо от значения (см. примечание к полкам).

### 4. Записи на старте (что шлёт программа при инициализации/START)
Наблюдались записи `0x10` по адресам: `0x3C(60)` пара с `61`, `0x3F(63)`, `0x5B(91)`,
`0x64(100)`, `0x96(150)`, `0x1FAC(8108)`. Значениями 0 или 1. Смысл записей в
`91`, `100`, `150`, `8108` не установлен (эмулятор принимает их как «успех» и игнорирует).

